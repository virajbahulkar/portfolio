---
import { BlogGallery } from '@components/BlogGallery';
import { PaginationHeader } from '@components/PaginationHeader';
import { Footer } from '@partials/Footer';
import Base from '@templates/Base.astro';
import { AppConfig } from '@utils/AppConfig';
import { sortPostByDate } from '@utils/Posts';
import { getCollection } from 'astro:content';
import { Section } from 'astro-boilerplate-components';
import CTA from '@sections/CTA.astro';

const allPosts = await getCollection('post');
const sortedPosts = sortPostByDate(allPosts);

const titleSuffix = ` - ${AppConfig.site_name}`;
const titleBase = `Blogs`;
const title = titleBase + titleSuffix;
const description =
  'Technical writing on backend systems, cloud-native delivery, observability, performance, and production engineering lessons.';
const url = `${AppConfig.site_url}/blogs/`;
---

<Base head={{ title, description, url }}>
  <Section>
    <PaginationHeader
      title="Writing on software, backend, and production systems"
      description="Articles focused on real engineering work across backend delivery, cloud-native systems, Docker, Kubernetes, observability, and lessons learned from production environments."
    />

    <div class="mb-12 mt-8 flex flex-col items-center space-y-4">
      <div class="circle-animation"></div>
      <p class="max-w-xl text-center  text-lg">
        I use writing to document engineering tradeoffs, production lessons,
        and implementation details that sit between application development and
        reliable infrastructure.
      </p>
    </div>
  </Section>

  <Section>
    <div class="grid grid-cols-1  gap-6 p-4">
      <BlogGallery postList={sortedPosts} />
    </div>
  </Section>

  <div class="flex-col lg:grid lg:grid-cols-2 overflow-auto p-6 lg:flex">
    <CTA />
  </div>
  <Footer />
</Base>
